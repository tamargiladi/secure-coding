# Educational Project

This is a monorepo for an educational project.

## Packages

- `packages/react-app`: A placeholder React application built with Vite.

## Getting Started

1. Install dependencies: `npm install`
2. Start the React app: `cd packages/react-app && npm run dev`

The React app will be available at http://localhost:5173/